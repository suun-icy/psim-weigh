# 2026-07-04 开发日志

## 功能1: 脱机手动模式（一期优先-1）

**状态**: ✅ 已完成

**变更文件**:
- `pism-weigh/Form1.Designer.cs` — 新增 `checkBoxManualMode` (CheckBox) 和 `labelManualTip` (Label) 控件，添加至 panel3
- `pism-weigh/Form1.cs` — 修改 `button2_Click`（称取重车）和 `button4_Click`（称取空车），新增 `checkBoxManualMode_CheckedChanged` 事件

**设计方案**:
- 在 panel3 底部新增"手动模式"CheckBox
- 选中时：textBox6 禁用，用户直接在 textBox1/textBox2 输入重量；button2/button4 做确认而非抓取
- 未选中时：保持原有联机逻辑，零改动
- 状态栏 label6 在手动模式下显示"手动模式"（橙色）

**兼容性**:
- 默认联机模式，原有业务流程完全不受影响
- 遵循开发规范：不修改已有逻辑、模块化设计、不强制依赖硬件
